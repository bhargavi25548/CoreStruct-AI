# CoreStruct-AI
A full-stack, privacy-focused web app that converts unstructured text into structured JSON using local AI. Built with React (Vite) and FastAPI, it extracts entities, sentiment, and actions with strict JSON accuracy, ensuring secure, on-device processing without external APIs.
